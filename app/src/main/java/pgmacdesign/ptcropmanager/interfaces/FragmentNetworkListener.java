package pgmacdesign.ptcropmanager.interfaces;

/**
 * Listener for use with network changes
 */
public interface FragmentNetworkListener {
    public void connectedToTheInternet(boolean isConnected);
}
