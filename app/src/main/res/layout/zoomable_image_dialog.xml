<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent"
    android:background="@color/black"
    >

    <com.github.chrisbanes.photoview.PhotoView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/zoomable_image_dialog_iv"
        />

    <ImageView
        android:layout_width="@dimen/square_image_sizing"
        android:layout_height="@dimen/square_image_sizing"
        android:id="@+id/zoomable_image_dialog_close_iv"
        android:src="@mipmap/close_x_white"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="4dp"
        android:layout_marginEnd="4dp"
        />


</RelativeLayout>